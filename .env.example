# Shared Supabase project ("kawabunga8's Project") — same one TOC-Dayplans,
# Student Hub, Report Card Tool, and Kawahoot use. Get these from the Supabase
# dashboard (Project Settings → API Keys) — never commit real values here.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=
SUPABASE_SECRET_KEY=
